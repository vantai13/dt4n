# ========================================================
# .gitignore cho DT4N — lá chắn an toàn + chống rác
# ========================================================

# --- BÍ MẬT: TUYỆT ĐỐI không commit (nhớ Lesson 0.6 + MASTER_PLAN an toàn) ---
.env
.env.*
!.env.example
*.key
*.pem
credentials
aws_credentials
**/secrets/
*.htpasswd        # nếu bạn đặt mật khẩu Ditto thật

# --- Python venv + cache (sinh tự động, không cần versioned) ---
venv/
.venv/
env/
**/venv/
**/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
node_modules/
dist/

# --- Log + file tạm ---
*.log
logs/*
!logs/.gitkeep
*.tmp
*.bak

# --- Rác của Mininet (nhớ Lesson 0.2/0.3: mạng rác, file sót) ---
*.pid
mininet.log
ditto/port_map.json

# --- Docker volume data nếu lỡ map ra thư mục local ---
mongo-data/
docker-volumes/

# --- File hệ điều hành / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Không ignore .gitkeep (giữ thư mục rỗng) ---
!.gitkeep

# Phase 4.5 raw logs (regenerable; artifacts/ and baseline/ are tracked)
docs/phase-4.5/raw/*.log
docs/phase-4.5/raw/*.txt
!docs/phase-4.5/raw/.gitkeep

# --- Phase 9+: chống rác quay lại ---
# Experiment outputs are local/regenerable. Commit only frozen_policies/ when a
# policy is officially frozen with provenance.
results/
runs/
!frozen_policies/**

# Hình phải sinh từ CSV bằng script, không commit hình vẽ tay
*.png
!docs/**/*.png        # trừ hình trong docs (dùng cho luận văn)

# Thư mục làm việc tạm
_graveyard/
_legacy/
scratch/
