# Kết quả thực hiện 20R2.9-A

Thực hiện ngày 2026-09-14 trên nhánh `main`, bắt đầu từ `3b69b29` sau E1. Phạm vi là A-1 đến A-6 theo hướng dẫn người dùng; chưa thực hiện các phép đo 20R2.9-B.

## Đã thay đổi gì?

| Mục | Thực hiện | Bằng chứng |
|---|---|---|
| A-2 | Giữ nguyên ý nghĩa `verdict`; thêm `complete`, `verdict_strict`. H8 sản xuất đọc strict và từ chối trường hợp thiếu tham số. | `test_20r2_9_h8_can_go_red.py`; [hygiene chạy lại](A-validation/hygiene-current.json) |
| A-5 | Ba CLI bắt buộc chọn chế độ; `--run` có nhánh riêng. `dt_s` truyền tường minh, bỏ rò `DT_S` khi import ở các công cụ liên quan; seed G5c đổi trong phạm vi chạy rồi trả lại. | Test CLI, import thứ tự đảo và khôi phục khi có exception; [log A5](A-validation/a5-green.log) |
| A-4 | 22 mặc định trục/7 file; mở rộng thành 94 mặc định hằng trục/đường dẫn artifact trên 35 file. Sentinel bắt lựa chọn thiếu trước IO. Cập nhật 128 biểu thức gọi trong lượt AST đầu và bổ sung các chỗ import nội bộ/kwargs phát hiện khi tích hợp. | [Danh sách mặc định ban đầu](A-validation/defaults-before.json), [128 caller đầu](A-validation/callers-explicit.json), [bảng bổ sung](A-validation/caller-integration-audit.json), [log A4](A-validation/a4-green.log) |
| A-3 | Tái sinh 9 JSON bằng producer, kiểm mọi giá trị ngoài `validity` trùng tuyệt đối; CSV trace trùng byte. 8 JSON giữ nguyên có lý do riêng và điều kiện hết hạn được test kiểm. | 9 `backfill-*.json`, [đối chiếu toàn bộ diff results](A-validation/changed-results-integrity.json), [log backfill](A-validation/backfills.log), [log N7/expiry](A-validation/a3-tests.log) |
| A-1 | Sổ gate, amendment chuyển D4 sang 21R2, custody lịch sử và kiểm chuỗi hash/trục thật. | [Sổ gate](99b-gate-ledger.md), [amendment](AMENDMENT-20R2-A2.md), [custody](../CUSTODY_LEDGER.json), [chuỗi trục](A-validation/axis-chain.json) |
| A-6 | Mở INV-01, đo diff g23-17c và truy lịch sử/danh tính đầu vào/phạm vi công bố. Giữ nguyên test và số kỳ vọng. | [INV-01](INV-01-g23-17c.md), [diff](A-validation/g23-17c-diff.json) |

## Kết quả chạy

| Lượt | FAIL | PASS | SKIP | DESELECT | Cảnh báo | Thời gian |
|---|---:|---:|---:|---:|---:|---:|
| Trước A | 25 | 3.739 | 200 | 13 | 2 | 874,52 s |
| Tích hợp, trước sửa caller lịch sử | 13 | 4.186 | 209 | 13 | 2 | 869,80 s |
| Cuối | **2** | **4.199** | **209** | **13** | **2** | **862,64 s** |

Kết quả theo tên test: **23/25 lỗi nền đã đóng, 2 lỗi nền còn mở, 0 lỗi mới**. Lệnh so sánh cùng phạm vi là `/home/ubuntu/miniforge3/envs/sdn_rl/bin/python -m pytest -q`. Theo `pytest.ini`, lần này bỏ marker `slow`/`custody`; các test `live` giữ guard sẵn có. Đây không phải lượt chạy mọi marker. Log [trước sửa](A-validation/full-before.log), [lần tích hợp](A-validation/full-after.log), [lần cuối](A-validation/full-final.log); [đối chiếu từng tên lỗi](A-validation/suite-comparison.json).

Đối chứng H8: bản chưa sửa có 7 FAIL/1 PASS trên nhóm 8 ca mới; sau sửa nhóm gate liên quan có 65 PASS. Nhóm N7 cùng expiry có 766 PASS/50 SKIP. Nhóm A1 và producer tái lập có 14 PASS/9 SKIP; 9 skip chính là D11. Các nhóm có phần chồng nhau và được chạy ở các thời điểm khác nhau, nên không cộng thành tổng suite cuối.

Hygiene chạy bằng `python -m tools.20r2_5_hygiene --out docs/phase-20R2/A-validation/hygiene-current.json`: H1-H8 PASS, H9 PASS; H8 kiểm **320/320** nhóm. H9 dùng thời gian chiến dịch lịch sử **70,0836502473 / 74,5285461744 = 0,9403598198**, không phải thời gian pytest. Xem [log màn hình](A-validation/hygiene-current.log).

Phép smoke đối chiếu metadata giữ dữ liệu bit-exact cho 4 cấu hình AoI: legacy U0 và measured U0/U1/U3; nhánh tau legacy có 19.989 hàng và bit-exact. [Artifact](A-validation/remediation-current.json) và [log](A-validation/remediation-current.log) được sinh bằng mã tích hợp cuối.

## Kết quả đo và giới hạn còn mở

- Custody prediction: **149 trường số, 5 trường đổi đều là số dòng nguồn**, thêm 5 thay đổi chuỗi/mô tả/thời điểm. Không giá trị dự đoán số nào đổi. Ledger tính lại byte tại commit thật và không chấp nhận riêng một cờ “không ảnh hưởng”.
- Chain: **4/13** tài liệu có ghim chuẩn tắc phân giải được; **9/13** vẫn nợ D11. H8 PASS không tự đóng gate 20R2-6(a): gate này vẫn **FAIL, mới sửa một phần truy nguyên**. Gate 20R2-1 là **SUBSTITUTED**, gate 20R2-7 **DEFERRED → 21R2**. Không có artifact literal v8 để xác nhận; `PHASE_20R2.md` được hướng dẫn viện dẫn nhưng không có bản nguồn trong checkout/lịch sử tìm được để đối chiếu.
- N7: 9 tái sinh + 8 miễn trừ có hạn dùng làm N7 hết lỗi; 8 artifact giữ nguyên chưa được gọi là có validity hay được phê duyệt. Grid T2 lịch sử có 96 verdict hẹp REALIZABLE nhưng **96 incomplete theo cách đọc strict**, đã khai trong `validity` mà không đổi verdict cũ.
- INV-01: **8 trường lệch**, lớn nhất **1,7763568394002505e-15**, không đổi dấu/check khoa học. Hai raw khớp hash công bố; raw poisson@0.925 đã đổi danh tính theo L85. Chưa tách nhân quả giữa môi trường số và thay đổi input, chưa xác minh luận văn ngoài repo; phiếu giữ **MỞ**.
- Lỗi nền `test_known_dangling_only_shrinks` thuộc phase-22: **7 parquet local** đã tồn tại nhưng còn trong danh sách thiếu và chưa được Git theo dõi. [Inventory/hash](A-validation/known-dangling-local-inventory.json) tách nợ lưu giữ này khỏi N7. Bước A không sửa danh sách hoặc di chuyển raw chỉ để test xanh.

## Tái lập và bảo toàn

Snapshot kiểm **206 artifact đã có trước bước A: 0 tệp thay đổi**. Chỉ có 9 tệp trong `results/` được A3 sửa; cả 9 đã được đối chiếu với `3b69b29` và chỉ khác khối top-level `validity`. Tag đóng phase `phase-20R2-closed^{}` vẫn ở `042c58f4dd20dc6f7b78cdfe7f3473a6e3745ffb`.

Các artifact ghi hash/số dòng nguồn được tái lập bằng mã lịch sử từ tag E1. Kiểm tra công cụ chạy tại HEAD vẫn được giữ. Prediction hiện tại còn được so nguyên giá trị khoa học với bản ký, chỉ cho năm trường số dòng dịch chuyển sau khi xác minh cả vị trí lịch sử lẫn hiện tại. Không loại bỏ số dự đoán để làm kiểm tra xanh. INV-01 giữ nguyên so sánh số tuyệt đối.

Các receipt backfill ghi argv, mã thoát, stdout/stderr, SHA trước/sau và xác nhận trùng payload ngoài `validity`. Lệnh tái chạy với đường dẫn đầu ra an toàn nằm trong [commands.txt](A-validation/commands.txt). Kết quả đo mới nằm trong `A-validation`; 9 JSON T2 tái sinh nằm trong `results/PENDING/phase-T2/`.
