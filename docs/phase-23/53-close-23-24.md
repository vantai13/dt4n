# DONG LESSON 23.24 -- KHONG GIAN HANH DONG HIEU DUNG

Tag        : `lesson-23-24-complete`
Commit     : `c65fe78` -> `be8a8a9` -> `51ed3d7` -> `744412c` -> `3632bcf`
             -> `32b9250` -> (commit dong lesson nay)
Amendment  : `A074` (tien dang ky) · `A074b` (sua `NC` TRUOC khi chay)
             · `A075` (khiem khuyet `w_loss`, thi hanh LAN HAI)
Doc chinh  : `52-action-pruning.md`
Artifact   : `results/LIVE/phase-23/action_pruning.json`
Ban VOID   : `results/SUPERSEDED/phase-23/action_pruning_VOID_wloss_defect.json`

## Ngan sach

```text
Da tieu   : 24 / 35 tuan (MASTER_PLAN_v8 PART II)
Con lai   : 5 lesson (23.25..23.29) + Phase 24 + Phase 25
Gate lesson nay: 4 / 4   (`A071` R1 -- DUNG ngan sach, khong mo them)
```

```text
Gate cap     : 4      `G23-297` .. `G23-300`
Gate dung    : 4
Lan thi hanh : 2      lan dau VOID (`w_loss` sai), lan hai tren du lieu dung
                      `A075` muc 2: chay lai KHONG tinh gate moi -- neu tinh
                      thi luat se THUONG cho viec khong sua loi
```

## 1. Phan quyet

```text
G23-297  M-233        PASS   tap CAT tren CALIB = {P2}, dung dai da ky
G23-298  M-234        PASS   ti so q_hat 0.923007 trong [0.88, 0.94]
                             Delta acceptance(S1) +0.042714 trong [+0.01, +0.05]
G23-299  M-235        PASS   budget_share(S1) 0.994144 >= 0.90
G23-300  NC-23.24-1   FAIL   ve (ii) -0.093702, dai doi >= +0.02
```

**Kich ban thi hanh: `K2`** (`A074` muc 6, ky TRUOC khi chay).

## 2. Phat bieu duoc phep

```text
DUOC  "Kenh NGAN SACH dong gop 99.41% cua muc acceptance tang khi cat hanh
       dong chet. Kenh RANG BUOC dong gop +0.36%, khong phan biet duoc voi 0."
DUOC  "Cau hinh chinh la K=3 (cat P2), theo tieu chi HAI TANG ky truoc:
       P_calib(a*) < 0.05 VA P_calib(a_twin) = 0."
DUOC  "Twin de xuat P2 voi xac suat <= 6.0e-06" (quy tac ba, n_calib=499798)
DUOC  "Bao dam bao phu BIEN giu o ca bon bac: viol_bien = 0.0789 / 0.0837 /
       0.0912 / 0.0844, tat ca <= alpha = 0.10."          [MO TA]

CAM   "bo mot hanh dong khong bao gio toi uu ... MA KHONG MAT BAO DAM NAO"
      -- `G23-300` FAIL, kich ban K2. `L43` KHONG dong duoc.
CAM   "twin khong bao gio de xuat P2"        -- phai dung chan 6.0e-06
CAM   "K = 2 la mot cau hinh de xuat"        -- hong tang 2(a)
CAM   doc `viol|accept` nhu mot bao dam co dinh ly  -- `L135`, `L136`
CAM   moi ngoai suy sang ho so AoI `U3`      -- `L133`, lesson nay do tren `U0`
```

## 3. `L43` VAN MO -- ly do chinh xac

`G23-300` FAIL **khong phai** vi he thong khong nhay, ma vi phep do KHONG
DUNG PHAM TRU: cat `P3` doi TAP NHAN (`P3` la `a*` tren 35.83% hang calib),
nen `err|accept` cua hai nhanh la hai dai luong tren HAI khong gian ket qua
khac nhau. `err_anchor` sup **0.244516 -> 0.050152** chung minh dieu do.

Day la mot van de PHAM TRU, khong phai van de do luong. Bat ky doi chung am
nao dua tren "cat mot hanh dong SONG" deu vap dung cho nay. Huong sua duy
nhat da biet: GHEP TAP NHAN -- chi so sanh tren tap hang ma duong bi cat
KHONG PHAI `a*` VA KHONG PHAI `a_twin`. Xem `L131`.

`A074` muc 6 ky truoc rang khong kich ban nao duoc dien giai lai sau khi
nhin so. `K2` duoc thi hanh NGUYEN VAN, ke ca khi co che that (`L131`) khac
voi chan doan ma `K2` viet san.

## 4. No mo ra

```text
L130  `cell_matrices.prepare()` chi dung duoc tren truc legacy        HOAN
L131  NC sai PHAM TRU -- can thiet ke lai theo huong ghep tap nhan    HOAN
L132  `w_loss` mac dinh ve truc DEPRECATED                            DONG (`A075`)
L133  `U0` vs `U3` -- pham vi da tuyen, `cell_matrices` khong sinh `U3`  HOAN
L134  `validity_block` nhan loi khai lam tham so                      DONG (R6)
L134b soat artifact CU xem cai nao khai truc sai (R6 khong hoi to)    HOAN
L135  `viol|accept` -- can tien dang ky dai                           HOAN
L136  `CL-06`/`CL-08` gan chu "bao dam" vao dai luong sau chon loc    HOAN
```

`L136` co cot (b) KHAC RONG, nen theo `A071` R2 no DU DIEU KIEN mo thanh mot
lesson. Ngan sach 23.24 da het `4/4` nen no PHAI vao hang doi. Dung muc dich
cua `A071`.

Da lam ngay, khong ton gate: them dong **CAM** vao `CLAIMS.md` muc "Phat bieu
bi CAM", ghi ro `CL-06`/`CL-08` hien dang trich `viol|accept`.

## 5. Bai hoc quy trinh

```text
1. `cell_matrices` MAC DINH ve mot truc DEPRECATED. Mac dinh im lang la dang
   loi nguy hiem nhat: khong exception, khong canh bao, chi mot thang chi phi
   sai 1.5517 lan.
   -> R6, va nguyen tac "moi tham so TRUC phai TUONG MINH tai diem goi".

2. `validity_block` nhan LOI KHAI lam THAM SO nen VE NGUYEN TAC khong the
   phat hien lech -- no chi chep lai dieu nguoi goi noi. Artifact VOID khai
   `sla_axis = exogenous_g114_S-B` va `w_loss = 5000.0` trong khi du lieu
   dung nguon khac.
   -> R6: moi gia tri ma du lieu CO THE tu khai thi PHAI doc tu du lieu.

3. ★ Khiem khuyet `w_loss` KHONG bi bat boi test nao, KHONG bi bat boi
   `validity`, KHONG bi bat boi bon dai da ky. No bi bat vi co MOT DAI LUONG
   DANG LE PHAI BANG NHAU giua hai lesson: `S0_K4` (khong cat gi, `alpha/3`,
   `selective`, `kappa=0.5`, cung cell) DUNG RA la chinh `C3` cua 23.23.
   -> MOI lesson sau nen co it nhat MOT dai luong trung voi mot lesson cu,
      dat CO Y, de lam DIEM NEO doi chieu ngang.

4. Bang so trong doc duoc CHEP TAY, va lan dau chep sai 4 o. Doc la thu
   nguoi doc tin, khong phai artifact.
   -> `test_doc52_ladder_table_matches_artifact`: doc va artifact la hai
      duong doc lap phai chi ve cung mot so. Cung tinh than R6.

5. Chu va so phai khop PHAM TRU, khong chi khop gia tri. "Bao dam bao phu"
   la ngon ngu DINH LY; `viol|accept` la dai luong SAU CHON LOC khong co
   dinh ly. Ba lan lesson nay vap vao cung mot chuyen: `L131` (tap nhan doi),
   `L135` (bao dam bien vs co dieu kien), `L136` (`CL-06`/`CL-08`).
```

## 6. Tai tao

```bash
.venv/bin/python -m pytest test/test_action_pruning.py -q
.venv/bin/python -m cert.action_pruning --dead    # G23-297, DUNG lai doc
.venv/bin/python -m cert.action_pruning --run     # G23-298..300
```

Thoi gian may: `--dead` 5.0 s, `--run` 17.3 s tren may tac gia.
Trinh thong dich la `.venv/bin/python` (`L117`).
